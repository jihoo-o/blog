import React from 'react';

interface PageTemplateProps {
    left?: JSX.Element;
    right?: JSX.Element;
    children?: JSX.Element;
}

const PageTemplate = (props: PageTemplateProps) => <div>PageTemplate</div>;

export default PageTemplate;
